9.19 接到项目，开始了解LMP相关概念，寻找数据
9.20 整理好一周的数据（2018.9.01-2018.9.07），开始阅读相关文献
9.21 从PJM重新获取、整理有分区信息的数据，选择使用MLP模型进行预测，开始编写程序
9.22 完成MLP模型的建立，使用AEP区9月1日和9月2日的模型，但是发现loss曲线一直在震荡，且没有下降趋势，观察数据后发现数据相似度极高，判断是由于相似度太高导致有效样本过少，而且MLP模型在处理时间序列上的效果似乎不甚理想
9.23 尝试了其他时期更大样本数据之后，loss曲线未得到改善，考虑改用RNN模型，以期达到对时间序列更好的预测效果。在使用RNN之前，对MLP参考康重庆老师的文章改变了input的形式，但是效果没有任何改观，怀疑是模型编写有问题
9.24 调整了AdamOptimizer的学习率之后，loss曲线有下降趋势，但是十分不稳定，会出现尖峰跳变
9.25 将MLP的层数加到2层，表现稳定了一些，但是仍然有尖峰，怀疑是神经元饱和所致
9.29 将sigmoid函数改为relu函数，loss曲线得到一定程度改善，可能是由于sig函数容易导致神经元饱和；开始预测电价，发现模型可以较好地得出电价变化趋势，但是对尖峰电价预测效果很差，于是重新开始查找、阅读文献
10.3 造成数据错位的原因：数据归一化、以及batch似乎会有影响
10.4 使用数据特征:当前时刻价格，前一天同一时刻价格，前6天同一时刻价格（与预测日相类似），预测日负荷，预测时间
可以考虑加入的特征：预测日类别？weekday & weekend
尝试了MLP和SVR，mape分别在13.4%和12%左右
10.5 MLP参数：learning rate、hidden layers、hidden nodes、exitation 	function、batch_size、iter number
SVM参数：C、gamma
XGB参数：reg_lambda、gamma
LGBM参数：reg_lambda、 num_leaves、 max_depth
MLP在初步调参之后error可以达到13.4%(不太稳定)
SVM在初步调参之后error可以达到12.8%（收敛很快，与xgb差不多）
xgboost在初步调参之后error可以达到8.7%（收敛很快）
lightgbm在初步调参之后error可以达到9.2%（收敛最快）
通过挑选出最重要的 Feature，可以将它们之间进行各种运算和操作的结果作为新的 Feature，可能带来意外的提高。
Feature Selection 最实用的方法也就是看 Random Forest 训练完以后得到的 Feature Importance 了。
加入新特征（cop、mlp）之后lightgbm的时间显著增加,且SVM误差率下降2%
神经网络误差大增但是尖峰效果似乎变好
目标任务：使用随机森林来查找较为重要的特征、模型融合、尝试解决尖峰问题、交叉验证、网格寻优、时间序列方法尝试
10.6 为了优化尖峰问题，设想将电价分梯度，按电价高低分为四个档次，并将档次标签作为特征，对于未知电价的测试集，其标签通过对load的分类产生，此处使用一个小型分类器，根据训练集的load-spike_mark对应关系进行分类